{
  "commands.category": "LabVIEW VI",
  "commands.helloWorld": "Hello World",
  "editor.viEditor.displayName": "LabVIEW VI Preview",
  "config.pythonPath.description": "Path to the Python executable used to invoke the bundled prototype scripts. Leave empty to use the system default (`python` on Windows, `python3` elsewhere).",
  "config.scriptTimeoutMs.description": "Timeout in milliseconds applied to each invocation of the bundled Python scripts (image export / property read / property write)."
}
